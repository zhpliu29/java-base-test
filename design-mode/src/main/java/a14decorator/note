

引入【装饰器模式】

定义：
    动态的给一个对象增加一些额外的职责，就增加对象的功能来说，装饰器模式比生成子类实现更为灵活。

角色：
    抽象构件：具体构件和抽象装饰器类的共同父类
    具体构件
    抽象装饰类：维护一个指向抽象构件对象的引用，通过该引用可以调用装饰之前构件对象的方法，并通过其子类扩展该方法，达到装饰的目的
    具体装饰类

优点：
缺点：
    排错难，对于多次装饰的对象，调试时寻找错误可能需要逐级排查，较为繁琐

适用场景：
    在不影响其他对象的情况下，以动态、透明的方式给耽搁对象添加职责