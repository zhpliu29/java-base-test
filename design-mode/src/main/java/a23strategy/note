before
1、calculate 过于庞大，出现较长的if...else...
2、增加新的算法需要修改MovieTicket类源码

引入【策略模式】

定义：
    定义一系列算法类，将每个算法封装起来，并让它们可以互相替换。该模式让算法独立于使用它的客户而变化。

角色：
    环境类：使用算法的角色，在环境类中维持一个对象策略类的引用实例
    抽象策略类
    具体策略类


优点：
缺点：


适用场景：
